schema_version: 2.0.0
name: stub-what-fixture
vision: Negative fixture exercising R15 length threshold — task.what under 80 chars triggers exit 1.
users:
  - validator under test
success_criteria:
  - id: SC-1
    description: validator exits 1 with minimum length error when what is short
scope:
  in_scope: []
  out_of_scope: []
tech_stack:
  - python3
existing_infrastructure_to_reuse: []
test_targets:
  - id: dotfiles
    path: .
setup:
  prerequisites: []
  runtime_dependencies: []
  services_to_provision: []
  environment_verification: []
  out_of_scope: []
kill_criteria: []
design_notes: []
risks: []
phases:
  - id: foundation
    name: Foundation
    overview_summary: Negative coverage.
    sequencing_rationale: Stub-string fixture asserts the 80-char minimum on what so the producer cannot ship 16-char placeholders.
    tasks:
      - id: stub-task
        name: Stub task
        task_type: development
        status: pending
        what: Implement feature
        why: This task intentionally has a 16-character what string to trigger the R15 minimum-length pattern check that requires at least 80 characters.
        where:
          modify:
            - claude/tools/x.py
        acceptance:
          - When validate-plan.py runs, the system shall exit 1 with a minimum-length error.
        prompt: stub
