# Copy to secrets/openai.env and replace the placeholder with your real key.
# Used by sync.sh diff --explain to invoke gpt-4o-mini for plain-language
# diff explanations. Never commit the real key.
OPENAI_API_KEY=sk-replace-me
