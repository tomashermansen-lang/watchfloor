schema_version: 2.0.0
name: oversized-acceptance-fixture
vision: Negative fixture — task with 6 acceptance entries trips R-A1 hard cap.
users:
  - validator under test
success_criteria:
  - id: SC-1
    description: validator exits 1 with R-A1 acceptance-count error
scope:
  in_scope: []
  out_of_scope: []
tech_stack:
  - python3
existing_infrastructure_to_reuse: []
test_targets:
  - id: dotfiles
    path: .
setup:
  prerequisites: []
  runtime_dependencies: []
  services_to_provision: []
  environment_verification: []
  out_of_scope: []
kill_criteria: []
design_notes: []
risks: []
phases:
  - id: foundation
    name: Foundation
    overview_summary: Negative coverage for R-A1.
    sequencing_rationale: walking-skeleton — chosen so the validator catches the over-cap acceptance count
    tasks:
      - id: oversized-task
        name: Oversized acceptance task
        task_type: development
        status: pending
        what: This task is sufficiently long but declares 6 acceptance entries which trips the R-A1 hard cap of 5 acceptance criteria.
        why: Over-decomposition of a single task into too many acceptance entries is a sizing violation that the producer self-review must catch and propose splitting via Pattern 7.
        where:
          modify:
            - claude/tools/x.py
        acceptance:
          - When the validator runs, the system shall exit 1.
          - When ac=2, the system shall flag the task.
          - When ac=3, the system shall flag the task.
          - When ac=4, the system shall flag the task.
          - When ac=5, the system shall flag the task.
          - When ac=6, the system shall flag the task.
        prompt: oversized-acceptance
        estimate:
          lines_estimate: 150
          duration_hours: 2
