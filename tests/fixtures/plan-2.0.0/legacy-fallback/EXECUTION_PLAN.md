# Execution Plan — legacy-fallback

## First legacy task

This is a 1.x markdown sibling consumed by the heading-match fallback in
the plan-detection skill. Its presence is the regression guard for R31.
