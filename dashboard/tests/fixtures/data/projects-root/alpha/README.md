# alpha (hermetic test project)

Synthetic project used by `test_response_compat.py` to exercise the
data-dependent dashboard endpoints. Contents are deterministic — do not
edit without recapturing the response baseline.
