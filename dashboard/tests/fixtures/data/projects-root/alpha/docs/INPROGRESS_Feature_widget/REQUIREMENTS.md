# Hermetic widget feature

Synthetic in-progress feature used to exercise `/api/features` discovery.

## Acceptance

- WHEN the dashboard scans docs/, THEN this feature appears under the
  inprogress lifecycle keyed by branch `feature/widget`.
